package g63551.dev3.oxono.model;

/**
 * Represents the possible states of the game.
 */
public enum GameState {

    INSERT, // State when a player can insert a token
    MOVE;   // State when a player can move a token
}
